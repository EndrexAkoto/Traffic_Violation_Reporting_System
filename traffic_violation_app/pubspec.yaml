name: traffic_violation_app
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  async: ^2.11.0
  collection: ^1.18.0
  cupertino_icons: ^1.0.8
  http: ^1.2.2
  google_maps_flutter: ^2.10.0
  image_picker: ^1.0.0+3
  url_launcher: ^6.0.9 # Moved to dependencies
  font_awesome_flutter: ^10.0.0
  fl_chart: ^0.40.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/dp.jpg
